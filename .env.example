# Local environment placeholders only.
# Copy to `.env` if a later task needs local keys.
# Never commit real credentials.

# RevenueCat (future; unused during repository bootstrap)
REVENUECAT_API_KEY=
REVENUECAT_PUBLIC_SDK_KEY=
